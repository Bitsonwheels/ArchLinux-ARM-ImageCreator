- pipeline: build
  name: Build
  refs:
  - refs/heads/main
  priority: LOW
  fail_on_prepare_env_warning: true
  resources: NANO
  arch: ARM64
  actions:
  - action: WORKDIR /source
    type: BUILD
    docker_image_name: archlinux/archlinux
    docker_image_tag: latest
    execute_commands:
    - ""
    - "# /source - Should mount the user code."
    - "VOLUME [ \"/source\" ]"
    - ""
    - "# Change the Workdir to /source"
    - WORKDIR /source
    setup_commands:
    - pacman -Syu
    - pacman -S git file awk gcc --noconfirm
    shell: BASH
